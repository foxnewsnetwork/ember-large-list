{{#model-aff this.getGames as |state|}}
  {{#if state.status.isSuccess}}
    {{reducer-animation/reducer-page model=state.model}}
  {{else if state.status.isError}}
    Encounter error
  {{else if state.status.isBusy}}
    Now Loading...
  {{/if}}
{{/model-aff}}